# Publish and export sanitized archive projections

SukimaCanvas will keep authoritative board archives private and expose only separately generated, sanitized projections: a read-only Published Canvas for allowed audiences and attribution-free PNG Image Exports for authorized organizer members. Raw SVG, engine previews, and client-side download paths will not be public service interfaces, preventing private attribution and audit metadata from escaping through legacy WBO asset routes.
